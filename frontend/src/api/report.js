// P1 (Week 5): GET /api/v1/report/latest — 诊断报告 API 封装
// 前置条件：A 的后端 /api/v1/report/latest 接口已提供
// 当前阶段：空壳，联调时补充实现

import request from './request'

// export function getLatestReport(device_id) {
//   return request.get('/report/latest', { params: { device_id } })
// }
