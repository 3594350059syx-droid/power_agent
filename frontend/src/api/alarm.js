// P0-3 (Week 3): GET /api/v1/alarm/list — 告警列表 API 封装
// 前置条件：A 的后端 /api/v1/alarm/list 接口已提供 + B 的数据库有告警记录
// 当前阶段：空壳，联调时补充实现

import request from './request'

// export function getAlarmList(params) {
//   return request.get('/alarm/list', { params })
// }
